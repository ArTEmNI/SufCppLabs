#include <iostream>

void inp() {
    std::cout << R"(Благосклоннейшая из муз, осенённая даром чисел! Снизойдите до беседы с сим механическим арифмометром — сей диковинной шкатулкой, чья душа есть чистая математика.

Соблаговолите обратиться к клавишам из слоновой кости и начертать на них единственное число, избранное вашей просвещённой волей.
Пусть то будет натуральное число — целое и неомрачённое дробями, подобно счёту жемчужин в ожерелье или лепестков на розе.

Когда вы совершите сей числовой жест, машина, повинуясь законам предустановленной гармонии,
отыщет в своих хрустальных глубинах тот единственный элемент числовой последовательности, что соответствует избранному вами номеру.
И явит его вам в сияющем окошке — как алхимик являет золото из первоэлементов.

Ваш выбор числа — суть ключ к тайне, сударыня. Позвольте же механическому интеллекту продемонстрировать вам чудо точности и предопределённости.

С глубочайшим пиететом к вашему разуму, покорный слуга математических искусств.)";
    std::cout << '\n';
}

int out(char n) {
    std::cout << '\n';
    std::cout << R"(Сударыня, осмелюсь доложить, что механический нумеролог совершил своё таинство! Внемлите, молю, сему откровению:

Тот элемент последовательности, коий вы столь милостиво повелели отыскать по избранному вами числу, ныне явлен в хрустальном окошке сей счётной машины.
Взирайте — вот он, пребывающий в одиночестве своей математической истины, сей единственный цифровой феникс, восставший из огня вычислений!

)";
    std::cout << n;
    std::cout << R"( — Сей знак есть не что иное как откровение, сударыня. Он занимает в числовом хороводе место,
коему вы сами определили почёт силою вашего указания. Цифра в нём отчеканена с точностью, каковой позавидовал бы и часовщик короля.

Сей результат есть чистейшее порождение разума, как сонет, рождённый в тиши кабинета, — неизменный и совершенный в своей предопределённости.
Позвольте вам предложить — не есть ли сие маленькое чудо достойно того, чтобы быть занесённым в свиток ваших учёных занятий?

С совершеннейшим почтением к вашему проницательному уму, остаюсь вашим покорным слугой в мире цифр и механических диковин.)";
    std::cout << '\n';
}

int main() {
    inp();

    long long cur = 1;
    long long pass = 0;
    int k;
    std::cin >> k;

    while (true) {
        std::string n = std::to_string(cur * cur);

        for (int i = 0; i < (int)n.size(); i++) {
            pass++;

            if (pass == k) {
                out(n[i]);
                return 0;
            }
        }
        cur++;
    }
}